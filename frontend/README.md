# ShortLink Frontend

Next.js TypeScript dashboard app for ShortLink.

## Commands

```bash
npm run dev
npm run lint
npm test
```

The dashboard connects to the ShortLink API and hosted authentication flow.

The infrastructure stack deploys the static export from `frontend/out`, so run `npm run build` before CDK synth/deploy.
